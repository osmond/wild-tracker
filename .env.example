# ─── API Keys ────────────────────────────────────────────────────────────────
# The Odds API        (https://the-odds-api.com) — required
ODDS_API_KEY=your_odds_api_key_here

# SportRadar NHL API  (https://developer.sportradar.com) — optional, no longer used
# The app now uses the free NHL public API (api-web.nhle.com) for schedules/results.
# SPORTRADAR_API_KEY=your_sportradar_api_key_here

# ─── Email Alerts (optional) ─────────────────────────────────────────────────
# Address that will receive sharp-move and settle notifications
ALERT_EMAIL=you@example.com

# Gmail account used as the sender — must have 2FA + an App Password configured
# https://support.google.com/accounts/answer/185833
GMAIL_USER=yourbot@gmail.com
GMAIL_APP_PASSWORD=your_16_char_app_password_here

# ─── Optional ────────────────────────────────────────────────────────────────
# Port the Express server listens on (default: 3000)
PORT=3000

# Secret key required to call /api/sync, /api/poll-odds, /api/settle
# Send as: Authorization: Bearer <your_key>
# If unset, these endpoints are unrestricted (fine for local dev, set in prod)
# ADMIN_KEY=change_me_to_a_long_random_string

# Absolute path to the SQLite database file (default: ./data/wild.db)
# DB_PATH=/absolute/path/to/wild.db

# Timezone for cron jobs (default: America/Chicago)
# CRON_TZ=America/Chicago
