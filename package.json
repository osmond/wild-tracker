{
  "name": "wild-odds-tracker",
  "version": "1.0.0",
  "description": "Minnesota Wild moneyline odds tracker — schedule, CLV, EV, results",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node test.js",
    "client:dev": "cd client && npm run dev",
    "client:build": "cd client && npm run build",
    "client:preview": "cd client && npm run preview",
    "client:install": "cd client && npm install"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "better-sqlite3": "^12.8.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-cron": "^3.0.3",
    "nodemailer": "^8.0.2"
  }
}
